sudo: required
dist: trusty
language: python
python:
  - "3.6"
before_install:
    - sudo apt-get install -qq python-numpy python-scipy
install:
  - pip install tqdm
  - pip install matplotlib
  - pip install stl
  - pip install trimesh
  - pip install scikit-image
